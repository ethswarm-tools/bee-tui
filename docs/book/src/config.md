# Configuration

See the [README config example](https://github.com/ethswarm-tools/bee-tui#quickstart). Stub — to be expanded with the full `[ui]` schema, `@env:VAR` token resolution rules, `[[nodes]]` precedence, and the search path bee-tui walks for `config.toml`.
