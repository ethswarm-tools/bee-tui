//! Re-export stub. The stamp-preview math + formatting moved to
//! [`bee_cockpit_core::stamp_preview`]; this stub preserves the
//! `crate::stamp_preview::*` paths inside bee-tui.

pub use bee_cockpit_core::stamp_preview::*;
