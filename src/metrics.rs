//! Re-export stub. The Prometheus exposition-format renderer moved
//! to [`bee_cockpit_core::metrics`]; this stub preserves the
//! `crate::metrics::*` paths inside bee-tui.

pub use bee_cockpit_core::metrics::*;
